export type OriginalPost = {
  title: {
    rendered: string;
  };
  excerpt: {
    rendered: string;
  };
  content: {
    rendered: string;
  };
  tags: number[];
  jetpack_featured_media_url: string;
};
